#include <bits/stdc++.h>
using namespace std;

int main() {
    long long N;
    cin >> N;

    // Calculate the summation using the formula (N * (N + 1)) / 2
    long long summation = (N * (N + 1)) / 2;

    cout << summation << endl;

    return 0;
}
